// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true, // Build ke waqt eslint errors ignore ho jayenge
  },
};

module.exports = nextConfig;
